const startTrack = document.querySelector('button.startTrack');
const stopTrack = document.querySelector('button.stopTrack');
startTrack.addEventListener('click', () => {
    // var cursorPosition = electron.screen.getCursorScreenPoint();
    // console.log('x: ' + cursorPosition.x);
    // console.log('y: ' + cursorPosition.y);
});
